{
  "bsonType": "object",
  "properties": {
    "nickname": { "bsonType": "string", "description": "昵称" },
    "avatar": { "bsonType": "string", "description": "头像" },
    "mobile": { "bsonType": "string", "description": "手机号" },
    "openid": { "bsonType": "string", "description": "openid" },
    "role": { "bsonType": "string", "enum": ["user", "admin"], "description": "角色" },
    "token": { "bsonType": "string", "description": "登录token" },
    "token_expire": { "bsonType": "number", "description": "token过期时间戳" }
  },
  "permission": {
    "read": true,
    "create": true,
    "update": true,
    "delete": false
  }
}